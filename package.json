{
  "name": "glyph-portal",
  "private": true,
  "version": "1.0.0",
  "description": "glyph.suns.red — portail des préviews web des Glyph Toys.",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "echo \"Choisis une app : bun run dev:portal | dev:glyphcast | dev:sonoglyph | dev:glyphlapse | dev:glyphslot\" && exit 1",
    "dev:portal": "bun run --cwd apps/portal dev",
    "dev:glyphcast": "bun run --cwd apps/glyphcast dev",
    "build": "bun run --filter './apps/*' build",
    "check": "bun run --filter './apps/*' check"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.3",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
