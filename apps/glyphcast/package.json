{
  "name": "@glyph/app-glyphcast",
  "private": true,
  "version": "1.1.0",
  "description": "Convertit une image en rendu Glyph Matrix pour Nothing Phone (3) et (4a) Pro.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "dependencies": {
    "@glyph/kit": "workspace:*"
  }
}
